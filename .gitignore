node_modules/
dist/
.env.local
.env.local.*

# M2M canonical identity layer â€” kept local, never committed
.claude/canonical/

# Build artifacts — never commit
src/pages/*.zip
# Sovereign Stack local context, never commit
CLAUDE.local.md
CLAUDE.*.local.md
.claude/local/
